# V-SpeechFlow Python Dependencies

# Core dependencies for V-SpeechFlow
# Python 3.10+ required

# Speaker diarization (optional, requires HF token)
# Breaking changes in 4.0+:
# - numpy>=2.0.0 required (was 1.x in 3.x)
# - Uses torchcodec for audio I/O (not deprecated torchaudio backends)
# - Pipeline.from_pretrained uses 'token' parameter (was 'use_auth_token')
pyannote.audio>=4.0.0

# PyTorch ecosystem (Apple Silicon with MPS acceleration)
torch>=2.8.0
torchaudio>=2.8.0
torchcodec>=0.6.0  # Required by pyannote.audio 4.0+

# Audio I/O
PyAudio>=0.2.14  # Live microphone recording
numpy>=2.0.0     # Required by pyannote.audio 4.0+

# Optional utilities
rich>=10.0.0     # Terminal formatting
requests>=2.28.0  # HTTP requests for model update checks

# Development utilities
watchdog>=3.0.0  # Hot reloading for development

# GUI dependencies
PyQt6>=6.5.0     # GUI framework

# App bundling for macOS
py2app>=0.28.0   # Create standalone .app bundles